import { Button } from "@mui/base"

export default function Header() {
    return (
        <header>
            <h1>Snippets Next</h1>
            <Button className="MuiButton">New</Button>
        </header>
    )
}